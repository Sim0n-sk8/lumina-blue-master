"use client"

import localFont from "next/font/local";
import "./globals.css";
import { SiteSettingsProvider, useSiteSettings } from './context/SiteSettingsContext';
import { useState, useEffect } from "react";
import Head from "next/head";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});

const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

function LayoutContent({ children }) {
  const { siteSettings, isLoading } = useSiteSettings();
  const [title, setTitle] = useState("Lumina Blue");

  useEffect(() => {
    if (siteSettings?.name) {
      setTitle(siteSettings.name);
    }
  }, [siteSettings]);

  return (
    <html lang="en">
     <Head>
  <title>{isLoading ? "Loading..." : title}</title>
  <meta name="description" content="Generated by create next app" />
  {siteSettings?.banners?.[0]?.bannerImg && (
    <meta property="og:image" content={siteSettings.banners[0].bannerImg} />
  )}
</Head>

      
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased flex flex-col min-h-screen w-full`}>
        <main className="flex-grow w-full relative">
          {children}
        </main>
      </body>
    </html>
  );
}

export default async function RootLayout({ children }) {
  const siteSettings = await fetchSiteSettings(); // ✅ now valid
  const title = siteSettings?.name ?? "Lumina Blue";
  const bannerImg = siteSettings?.banners?.[0]?.bannerImg ?? "/default-banner.png";

  return (
    <html lang="en">
      <head>
        <title>{title}</title>
        <meta name="description" content="Generated by create next app" />
        <meta property="og:image" content={bannerImg} />
      </head>
      <body className="antialiased flex flex-col min-h-screen w-full">
        {children}
      </body>
    </html>
  );
}

